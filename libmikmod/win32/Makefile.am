# autotools discourages adding directories to EXTRA_DIST,
# but I'm doing it to indclude the MSVC project files.
EXTRA_DIST = Makefile.lcc Makefile.mingw \
	     mikmod.def mikmod.h mikmod_build.h mikmod.h.in mikmod_build.h.in \
	     README CMakeLists.txt MSVC6 VS2005
